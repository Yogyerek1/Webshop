* sudo * docker compose up --build -> Build images and start containers.
* sudo * docker compose up -> Start containers and attach to logs.
* sudo * docker compose up -d -> Start containers in detached mode (background).